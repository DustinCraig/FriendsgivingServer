/* When this is clicked it's assuming that the players have entered their usernames */
const startClick = () => {
    
}

(() => {
    console.log('Setting up event listeners')
    /* Setup Event Listeners */
    document.getElementById('start_button').addEventListener('click', startClick)
})()
